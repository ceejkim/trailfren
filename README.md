# TrailFren main site

Built with Gatsby

Hosted on Netlify

## Deploy

1. build

        ```bash
        export NODE_ENV=production && npm run build.
        ```

2. deploy

        ```bash
        export NODE_ENV=production && npm run netlify:deploy
        ```
